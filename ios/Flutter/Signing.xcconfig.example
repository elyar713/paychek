// Copier vers Signing.xcconfig (gitignored) et remplacer par votre Team ID Apple.
// Team ID : Xcode → Settings → Accounts → [équipe Paychek] → Team ID (10 caractères).
// Obligatoire pour xcodebuild / flutter build ipa sur MacinCloud (CLI).
DEVELOPMENT_TEAM=XXXXXXXXXX

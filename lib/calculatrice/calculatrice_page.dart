// Keep this file intentionally small (< 300 lines).
//
// The actual Calculatrice UI lives in `calculatrice_page_impl.dart`.
//
// Legacy/duplicated content (if needed) is kept in:
// `calculatrice_page_legacy_dump.dart` (safe to delete later).

export 'calculatrice_page_impl.dart';

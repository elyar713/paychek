/// Coach AI visible dans l’app utilisateur (web / Android / iOS).
/// `false` = labo admin uniquement jusqu’à validation produit.
abstract final class CoachAiPublicEnabled {
  static const bool value = false;
}
